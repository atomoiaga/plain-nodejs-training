const writeHandler = require("./write");
const notFoundHandler = require("./notFound");
const encodeHandler = require("./encodeDecode");
module.exports = {
  writeHandler,
  notFoundHandler,
  encodeHandler,
};
